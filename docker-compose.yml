version: '3.8'

services:
  app:
    image: openjdk:17
    ports:
      - "8080:8080"

  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: tooldb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin

  redis:
    image: redis:7
    ports:
      - "6379:6379"